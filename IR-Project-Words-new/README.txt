README:

Words.py takes documents.txt and words.txt as input and creates a word hash. 
lyrics to bow.py takes the lyrics of a song as input and returns a bag of words to enable to query the word hash.
And_wordlists.py will find the intersections between the different lists and output the tp 10 results.